import 'package:flutter/material.dart';

class DuaVaultScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('My Duas'),
        backgroundColor: Colors.brown[700],
        titleTextStyle: TextStyle(color: Colors.white),
      ),
      body: Center(child: Text('Vault Screen - Coming Soon!')),
    );
  }
}
